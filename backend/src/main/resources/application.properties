spring.application.name=ai-smart-camera-security

# Dev database: file-based H2 (zero setup, persists between restarts in ./data/ai_smart_camera_security.mv.db)
spring.datasource.url=jdbc:h2:file:./data/ai_smart_camera_security;AUTO_SERVER=TRUE;NON_KEYWORDS=KEY
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Flyway only runs in the "prod" profile (see application-prod.properties) — the dev H2
# database is managed by Hibernate's auto-DDL (ddl-auto=update) above instead.
spring.flyway.enabled=false

# H2 console at http://localhost:8000/h2-console (dev only — disable in production)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Use snake_case JSON field names (is_bundle, stock_qty, full_name, etc.) to match
# what the React frontend expects
spring.jackson.property-naming-strategy=SNAKE_CASE

server.port=${PORT:8000}

# ---------------------------------------------------------------------------
# Admin back-office (http://localhost:8000/admin/)
# CHANGE ADMIN_USERNAME before deploying anywhere reachable from the internet.
# Leave ADMIN_PASSWORD unset and a secure random password is generated at startup
# and printed to the log once — see SecurityConfig. Set ADMIN_PASSWORD explicitly
# once you're ready to log in reliably across restarts.
# ---------------------------------------------------------------------------
admin.username=${ADMIN_USERNAME:AISmartCam}
admin.password=${ADMIN_PASSWORD:aismartcamera@170926}

# ---------------------------------------------------------------------------
# Order confirmation / merchant notification emails.
# mail.enabled=false (the default) logs emails to the console instead of sending
# them — no SMTP account needed to develop locally. Set mail.enabled=true and
# fill in real SMTP credentials (e.g. an app password for Gmail, or a
# transactional-email provider like SendGrid/Mailgun/Brevo) to actually send.
# ---------------------------------------------------------------------------
mail.enabled=${MAIL_ENABLED:true}
mail.from=${MAIL_FROM:aismartcamerasecurity@gmail.com}
mail.admin-address=${MAIL_ADMIN_ADDRESS:aismartcamerasecurity@gmail.com}

spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:aismartcamerasecurity@gmail.com}
spring.mail.password=${SMTP_PASSWORD:YOUR_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------------------------------------------------------------------------
# Product photo uploads via the admin panel, served back out at /media/**
# ---------------------------------------------------------------------------
uploads.dir=${UPLOADS_DIR:./uploads}
spring.servlet.multipart.max-file-size=8MB
spring.servlet.multipart.max-request-size=8MB

# ---------------------------------------------------------------------------
# PayFast payment gateway (https://developers.payfast.co.za)
# Sandbox defaults below are PayFast's own published test-merchant credentials —
# safe to commit, safe to use for testing, but they don't move real money.
# Get real credentials from your PayFast merchant dashboard before going live,
# set PAYFAST_SANDBOX=false, and put them in environment variables instead.
# ---------------------------------------------------------------------------
payfast.sandbox=${PAYFAST_SANDBOX:true}
payfast.merchant-id=${PAYFAST_MERCHANT_ID:10000100}
payfast.merchant-key=${PAYFAST_MERCHANT_KEY:46f0cd694581a}
payfast.passphrase=${PAYFAST_PASSPHRASE:jt7NOE43FZPn}
# Where the customer's browser returns to after paying/cancelling (frontend route)
payfast.return-url=${PAYFAST_RETURN_URL:http://localhost:5173/order}
payfast.cancel-url=${PAYFAST_CANCEL_URL:http://localhost:5173/order}
# Where PayFast POSTs the server-to-server payment confirmation (this backend)
payfast.notify-url=${PAYFAST_NOTIFY_URL:http://localhost:8000/api/payments/payfast/itn}

# ---------------------------------------------------------------------------
# VAT / invoicing details shown on the storefront and in order emails
# ---------------------------------------------------------------------------
store.vat-number=${STORE_VAT_NUMBER:}
store.vat-rate-percent=${STORE_VAT_RATE_PERCENT:15}
store.prices-include-vat=${STORE_PRICES_INCLUDE_VAT:true}

# ---------------------------------------------------------------------------
# Rate limiting (requests per minute per IP) on cart/checkout endpoints —
# see config/RateLimitFilter.java
# ---------------------------------------------------------------------------
ratelimit.requests-per-minute=${RATELIMIT_PER_MINUTE:60}

# ---------------------------------------------------------------------------
# Production database: see application-prod.properties for the Postgres +
# Flyway setup. Run with --spring.profiles.active=prod (or SPRING_PROFILES_ACTIVE=prod)
# to use it instead of the H2 dev database configured above.
# ---------------------------------------------------------------------------
